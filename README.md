# Notas:

```
$ node app --help
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     es la base de la tabla de multiplicar      [número] [requerido]
  -l, --listar   es la lista                         [booleano] [defecto: false]
  -h, --hasta    es el limite de numero por el que se va a multiplicar la base
                                                          [número] [defecto: 10]
```